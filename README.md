# Proyecto de Modificación del proyecto galois

# TODO:
Tienes que terminar este readme no se te olvide

# Iniciar el proyecto

1) Crear un archivo `.env` aquí en la raiz del proyecto que tenga una variable llamada `OPENAI_API_KEY` con la key de la API de OpenIA

2) Descomprimir `spider_files.zip` en su propia carpeta dentro de `data`